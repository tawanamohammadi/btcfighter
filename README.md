# 🔐 Bitcoin Wallet Corporate Tester

سرویس تست خودکار و حرفه‌ای ولت بیت‌کوین با قابلیت تولید و چک کردن موجودی به صورت موازی.

---

## 📋 قابلیت‌های کامل

### 1️⃣ **تولید ولت (Wallet Generation)**
- ✅ **استاندارد BIP39**: تولید کلمات بازیابی 12 کلمه‌ای
- ✅ **استاندارد BIP32**: مدیریت درخت کلیدها (HD Wallet)
- ✅ **استاندارد BIP44**: آدرس‌های Legacy که با `1` شروع می‌شوند
- ✅ **استاندارد BIP49**: آدرس‌های SegWit که با `3` شروع می‌شوند
- ✅ **استاندارد BIP84**: آدرس‌های Native SegWit که با `bc1q` شروع می‌شوند
- ✅ **آفلاین 100%**: تولید ولت نیازی به اینترنت ندارد
- ✅ **امنیت کامل**: از کتابخانه‌های رسمی bitcoinjs و tiny-secp256k1 استفاده می‌شود

### 2️⃣ **چک کردن موجودی (Balance Checking)**
- ✅ **دو API عمومی معتبر**:
  - `Blockchain.info` (محدودیت: ~300 req/5min ≈ 1 req/sec)
  - `Blockstream.info` (محدودیت: ~100 req/min ≈ 1.6 req/sec)
- ✅ **سیستم چرخشی (API Rotation)**: در صورت شکست یک API، به API دیگر سوییچ می‌کند
- ✅ **Timeout محافظتی**: هر درخواست حداکثر 5 ثانیه طول می‌کشد
- ✅ **خروجی دقیق**: موجودی به ساتوشی (Satoshi) گزارش می‌شود

### 3️⃣ **موازی‌سازی (Concurrency)**
- ✅ **Worker Thread**: 5 ترد موازی به صورت پیش‌فرض
- ✅ **Delay بین درخواست‌ها**: 200ms برای احترام به محدودیت API
- ✅ **قابل تنظیم**: می‌توانید تعداد ترد‌ها را تغییر دهید

### 4️⃣ **ذخیره‌سازی (Database)**
- ✅ **SQLite**: فایل `wallets.db` برای ذخیره یافته‌ها
- ✅ **فیلدها**: 
  - Mnemonic (کلمات بازیابی)
  - Address (آدرس ولت)
  - Balance (موجودی به ساتوشی)
  - Type (نوع آدرس)
  - Timestamp (زمان یافتن)
- ✅ **UNIQUE Constraint**: از ذخیره تکراری جلوگیری می‌کند

### 5️⃣ **رابط کاربری (UI)**
- ✅ **Progress Bar**: نوار پیشرفت با کتابخانه `cli-progress`
- ✅ **آمار لحظه‌ای**: تعداد چک شده و تعداد یافته شده
- ✅ **رنگی (Colorful)**: با استفاده از `chalk`
- ✅ **لاگ یافته‌ها**: به محض پیدا شدن ولت با موجودی، اطلاعات کامل نمایش داده می‌شود

---

## ⚡ سرعت و محدودیت‌ها

### 🚀 **وضعیت فعلی (با API‌های عمومی)**

| موضوع | مقدار | توضیحات |
|------|-------|---------|
| **سرعت تولید ولت** | ~50,000 ولت/ثانیه | کاملاً آفلاین، فقط محاسبات CPU |
| **سرعت چک موجودی** | ~5 ولت/ثانیه | محدودیت API عمومی (با 5 ترد) |
| **تعداد چک روزانه** | ~432,000 ولت | 5 × 60 × 60 × 24 |
| **Concurrency** | 5 ترد | قابل افزایش تا 10، بیشتر ریسک بن شدن دارد |
| **محدودیت Blockchain.info** | 300 req/5min | ~1 req/sec |
| **محدودیت Blockstream** | 100 req/min | ~1.6 req/sec |
| **ریسک بن شدن IP** | متوسط تا بالا | با بیش از 10 req/sec |

### 🔥 **وضعیت با Node اختصاصی (Bitcoin Core RPC)**

| موضوع | مقدار | توضیحات |
|------|-------|---------|
| **سرعت چک موجودی** | ~500 ولت/ثانیه | با 100 ترد موازی |
| **تعداد چک روزانه** | ~43,200,000 ولت | 500 × 60 × 60 × 24 |
| **Concurrency** | 100+ ترد | محدودیت فقط CPU و RAM |
| **محدودیت** | صفر | شما مالک سرور هستید |
| **ریسک بن شدن** | صفر | سرور شخصی |

### ⚡ **وضعیت با Electrum Servers**

| موضوع | مقدار | توضیحات |
|------|-------|---------|
| **سرعت چک موجودی** | ~100 ولت/ثانیه | با 50 ترد موازی |
| **تعداد چک روزانه** | ~8,640,000 ولت | 100 × 60 × 60 × 24 |
| **Concurrency** | 50 ترد | بسته به سرور Electrum |
| **محدودیت** | کم | بیشتر سرورها محدودیت ندارند |
| **ریسک بن شدن** | کم | قابل چرخش بین چند سرور |

---

## 📊 احتمال پیدا کردن ولت با موجودی

### **واقعیت ریاضی**:
```
تعداد کل ترکیبات BIP39 (12 کلمه): 2^128 ≈ 3.4 × 10^38
تعداد آدرس‌های بیت‌کوین فعال: ~50,000,000
احتمال پیدا کردن ولت فعال: ~1 در 6.8 × 10^30
```

**یعنی**:
- اگر **1 میلیارد ولت در ثانیه** چک کنید
- باز هم **215 میلیارد سال** طول می‌کشد تا یک ولت پیدا کنید

### **نتیجه‌گیری**:
این سرویس برای اهداف آموزشی، تحقیقاتی یا تست امنیت طراحی شده. پیدا کردن ولت واقعی با موجودی **عملاً غیرممکن** است.

---

## 🛠️ نحوه استفاده

### روش 1: اجرای ساده
```bash
npm start
```

### روش 2: تغییر تعداد ترد‌ها
کد `src/index.ts` را باز کنید و خط زیر را تغییر دهید:
```typescript
const CONCURRENCY = 5; // تغییر به 10 یا 20 (برای API عمومی توصیه نمی‌شود)
```

### روش 3: تغییر نوع آدرس
در `src/index.ts`:
```typescript
const wallet = WalletGenerator.generate('bech32'); // یا 'segwit' یا 'legacy'
```

---

## 📦 فایل‌های خروجی

### `wallets.db`
فایل SQLite که تمام ولت‌های با موجودی را ذخیره می‌کند:
```sql
SELECT * FROM found_wallets;
```

---

## 🚨 هشدارها

1. **قانونی**: استفاده از این ابزار برای سرقت غیرقانونی است.
2. **API Abuse**: استفاده بیش از حد از API‌های عمومی می‌تواند منجر به بن شدن IP شود.
3. **احتمال موفقیت**: عملاً صفر است (ریاضی ثابت شده).

---

## 🔮 پیشنهادات برای بهبود

### 1. **نصب Bitcoin Core Node**
- سرعت: 100 برابر فعلی
- محدودیت: صفر
- هزینه: سرور با 500GB فضا + 8GB RAM

### 2. **استفاده از Electrum Servers**
- سرعت: 20 برابر فعلی
- محدودیت: کم
- هزینه: رایگان

### 3. **Proxy Rotation**
- تغییر IP هر چند دقیقه
- استفاده از Tor یا VPN Pool

---

## 📞 پشتیبانی

آیا می‌خواهید:
- [ ] کد اتصال به **Bitcoin Core RPC** اضافه شود؟
- [ ] کد اتصال به **Electrum Server** اضافه شود؟
- [ ] سیستم **Proxy Rotation** پیاده‌سازی شود؟
- [ ] یک **Web Dashboard** برای مانیتورینگ ساخته شود؟

فقط بگویید کدام یک را می‌خواهید! 🚀
